def countingSort(arr):
    size=max(arr)+1
    result=[]
    for i in range(size):
      result.append(0)
      for j in range(len(arr)):
        if arr[j]==i:
            result[i]+=1
    return result
